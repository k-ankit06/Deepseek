# AI-ML Service Requirements - Ultra-Lightweight for Render Free Tier
# Uses ONLY OpenCV - no system dependencies needed (no apt-get)
# Total install size: ~50MB

# Flask server
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Image processing (OpenCV headless = no GUI/system deps)
opencv-python-headless>=4.8.0
pillow>=10.0.0

# NumPy (for array math)
numpy<2.0.0

# Utilities
python-dotenv>=1.0.0